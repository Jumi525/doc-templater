import React from "react";
import Converter from "../components/global/converter";

const page = () => {
  return (
    <section className="bg-gradient-to-br from-blue-200 to-blue-400 w-full h-full flex items-center justify-center">
      <Converter />
    </section>
  );
};

export default page;
